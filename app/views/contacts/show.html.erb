<%= @contact.name %>
<%= @contact.role %>
<%= @contact.phone %>
<%= @contact.email %>
<%= link_to "back to account", @contact.account %>
<%= button_to "delete contact", @account_path, method: :delete, data: { confirm: "Are you sure?"}  %>
